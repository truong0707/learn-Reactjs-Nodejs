import React from 'react';
import { SEE_MORE_CARD_BLOG } from '../Types'

export default function ProductsBlogReducer(state, action) {
    switch(action) {
        case SEE_MORE_CARD_BLOG: {
            return
        }
    }
}
